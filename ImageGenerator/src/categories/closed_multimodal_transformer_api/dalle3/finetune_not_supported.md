## Fine-tuning not supported: `dalle3`

This model is a **closed API** and supports **inference only** in this benchmarking suite.

If `finetune` or `run_finetuned` is called, the suite will raise a clear error.

